<UserControl
    x:Class="MapleStoryHelper.Control.PotentialControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MapleStoryHelper.Control.Item"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="White">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <controls:UniformGrid x:ConnectionId='3' x:Name="ufgPotential" Grid.Column="0" Columns="1">
            <ComboBox ItemsSource="{Binding PotentialItems}"/>
            <ComboBox ItemsSource="{Binding PotentialItems}"/>
            <ComboBox ItemsSource="{Binding PotentialItems}"/>
            <ComboBox ItemsSource="{Binding PotentialItems}"/>
            <ComboBox ItemsSource="{Binding PotentialItems}"/>
            <ComboBox ItemsSource="{Binding PotentialItems}"/>
        </controls:UniformGrid>
        <controls:UniformGrid x:ConnectionId='4' x:Name="ufgPotentialTextBox" Grid.Column="1" Columns="1">
            <TextBox Text=""/>
            <TextBox Text=""/>
            <TextBox Text=""/>
            <TextBox Text=""/>
            <TextBox Text=""/>
            <TextBox Text=""/>
        </controls:UniformGrid>

    </Grid>
</UserControl>

