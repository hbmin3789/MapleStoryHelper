<Page
    x:Class="MapleStoryHelper.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MapleStoryHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MapleStoryHelper.Control"
    xmlns:control="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" 
                       FontSize="25" 
                       Text="메이플 설치 경로 : "/>
            <TextBox Grid.Column="1" HorizontalAlignment="Stretch"
                     FontSize="30"
                     Text="{Binding WzFilePath, Mode=TwoWay}"/>
        </Grid>
        
        <Grid Grid.Row="1">
            <Button x:ConnectionId='2' x:Name="btnUnionSetting" HorizontalAlignment="Left" VerticalAlignment="Center"
                    Width="200" Height="50" Margin="30,0,0,0"
                    Content="유니온 설정하기"/>
            <Button x:ConnectionId='3' x:Name="btnDPMCalc" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Width="200" Height="50"
                    Content="예상 DPM 계산"/>
            <Button x:ConnectionId='4' x:Name="btnAddCharacter" HorizontalAlignment="Right" VerticalAlignment="Center"
                    Width="200" Height="50" Margin="0,0,30,0"
                    Content="캐릭터 추가"
                                                 />
        </Grid>

        <Grid Grid.Row="2">
            <controls:CharacterControl/>
        </Grid>
    </Grid>
</Page>

