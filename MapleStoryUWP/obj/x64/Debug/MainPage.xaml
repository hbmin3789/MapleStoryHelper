<Page
    x:Class="MapleStoryUWPPriceCalc.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MapleStoryUWPPriceCalc"
    xmlns:control="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:MapleStoryUWPPriceCalc.Converter"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <converter:PageTemplateSelector x:Key="PageSelector"
                                        ItemPrice="{StaticResource ItemPriceTemplate}"/>
    </Page.Resources>
    <Grid>
        <NavigationView MenuItemsSource="{Binding MenuItems}"
                        ContentTemplateSelector="{StaticResource PageSelector}"
                        FlowDirection="LeftToRight"
                        PaneDisplayMode="Top"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False">
            <NavigationView.MenuItemTemplate>
                <DataTemplate>
                    <Grid>
                        <NavigationViewItem/>
                    </Grid>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>
        </NavigationView>
    </Grid>
</Page>

